# 🔐 Exposed Secrets Audit — 2026-05-27

> Generated by: Echo (KiloClaw)
> Action required: Rotate keys listed below ASAP

---

## 🚨 Secrets Found in Public GitHub Repos

### 1. Cal.com API Key — echo-v1 (CRITICAL)

**Key:** `cal_live_ee5d46c871de452619a7388c674a3c7f`
**Found in:**

| File | Line | Notes |
|------|------|-------|
| `memory/2026-05-12.md` | 70 | Full key in plain text |
| `memory/2026-05-13.md` | 14 | Full key in plain text |
| `memory/2026-05-15.md` | 19 | Full key in plain text |
| `memory/2026-05-27.md` | 27 | Full key (Echo wrote this — fixed same day) |

**Risk:** Cal.com API key exposed in GitHub history. Anyone with repo access can use it to manage your calendar bookings, create/cancel events, or access client data.

**Action:**
1. Go to https://cal.com/settings/developer/api-keys
2. Delete key `cal_live_ee5d46c...`
3. Create new key
4. Update `/root/.secrets/cal_token` on KiloClaw
5. Update any Railway env vars that use it

---

### 2. OpenRouter API Key — alexander-ai-floodclaim

**Key:** `sk-or-v1-298819...` (partial — see `alexander-ai-floodclaim/scripts/browser_test_suite.py` line ~30)
**Found in:** `Liberty-Emporium/alexander-ai-floodclaim` → `scripts/browser_test_suite.py`

**Risk:** OpenRouter key exposed — can be used to make AI API calls charged to your account.

**Action:**
1. Go to https://openrouter.ai/keys
2. Delete the exposed key
3. The new key `sk-or-v1-c6f60974...` is already saved and deployed to Railway
4. Remove the old key from the test script or replace with env var reference

---

## 📋 Recommended .gitignore Additions for echo-v1

The `echo-v1` repo has no `.gitignore` that covers the `memory/` folder. Add this:

```gitignore
# Secrets in memory files — never commit full API keys
memory/*.md
memory/**/*.md
.env
.env.*
*.secret
/root/.secrets/
```

> Note: This won't remove already-committed history. Use `git filter-branch` or BFG Repo Cleaner to scrub the git history if needed.

---

## ✅ Keys Already Rotated This Session

| Key | Old Value | Status |
|-----|-----------|--------|
| OpenRouter (AI Studio) | `sk-or-v1-41e8f...` (floodclaim) | ✅ New key deployed to Railway |
| GitHub PAT | `ghp_kz4t8cOH3...` | ✅ Rotated — new token in place |

---

## 🔑 Current Active Secrets (stored in /root/.secrets/)

| File | Purpose | Rotation Priority |
|------|---------|-------------------|
| `github_token` | GitHub API access | Low (rotate after use) |
| `gitlab_token` | GitLab backup | Low |
| `railway_token` | Railway project ID | N/A (not an API token) |
| `ecdash_token` | EcDash bridge API | Low |
| `tailscale_authkey` | Tailscale node auth | Low |
| `tailscale_api_token` | Tailscale API | Low |
| `cal_token` | Cal.com API | **🔴 ROTATE NOW** |
| `openrouter_api_key` | AI Studio (new) | Low |
| `ecdash_admin_password` | EcDash admin login | Low |

---
_Next review: After Cal.com key rotation_
